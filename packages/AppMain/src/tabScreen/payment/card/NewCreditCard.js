export default class NewCreditCard {
    constructor(cardNo, cardHolder, ccv, expiryMonth, expiryYear) {
        this.cardNo = cardNo
        this.cardHolder = cardHolder
        this.ccv = ccv
        this.expiryMonth = expiryMonth
        this.expiryYear = expiryYear
    }
}
