export default class VoucherItem {
    constructor(title, description, startTime, deadline, label, value) {
        this.title = title
        this.description = description
        this.startTime = startTime
        this.deadline = deadline
        this.label = label
        this.value = value
    }
}
